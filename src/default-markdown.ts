export const defaultMarkdown = `# Markdown To HTML Preview

This workspace starts with a built-in example instead of loading a file.

## Features

- Edit Markdown on the left
- Inspect rendered HTML on the right
- Download the current Markdown
- Download the generated HTML document

## Example Code

\`\`\`ts
function greet(name: string) {
  return \`Hello, \${name}\`;
}
\`\`\`

> Replace this content with your own Markdown.
`;
